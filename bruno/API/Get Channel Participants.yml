info:
  name: Get Channel Participants
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/v1/channels/5/participants
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
