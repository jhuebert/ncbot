server:
  port: 8080

spring:
  application:
    name: ncbot
  datasource:
    url: jdbc:sqlite:ncbot.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.community.dialect.SQLiteDialect
  ai:
    openai:
      api-key: ${NCBOT_API_KEY:default-key}
      base-url: ${NCBOT_OPENAI_BASE_URL:http://192.168.1.240:11435/v1}
      chat:
        options:
          model: ${NCBOT_MODEL:ncbot}

ncbot:
  system-prompt: |
    You are ncbot, an AI assistant on the Meshcore mesh network.
    You are tasked to respond to the user message in a short and concise way.
    Your entire response should be no more than 128 UTF-8 bytes in length.
    Do not prefix your response with your name.
    Use the available tools to help you.
#    This should be minimum response delay
  response-delay-seconds: ${NCBOT_RESPONSE_DELAY_SECONDS:0.0}
#  It should also have a maximum response time and a string to return if it didn't respond quickly enough
  max-reply-bytes: ${NCBOT_MAX_REPLY_BYTES:128}
  history-limit: ${NCBOT_HISTORY_LIMIT:20}
  # Channel filtering — leave empty for all channels
  allowed-channels:
    - "#ncbot"
  # Whether to respond in DMs
  allow-dms: true

gg:
  jte:
    development-mode: ${JTE_DEV_MODE:false}
    use-precompiled-templates: true

logging:
  level:
    root: WARN
    org.huebert: INFO
